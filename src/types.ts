export type Entity={id:string;name:string;active:number;notes?:string;deleted_at?:string|null;[key:string]:any};
export type Plan=Entity&{source_id:string;target_id:string;software_id?:string;dataset_ids:string[];protection_type:'backup'|'synchronization'|'archive';schedule_type:'daily'|'weekly'|'monthly'|'manual';weekdays:number[];day_of_month?:number;start_time:string;duration_minutes:number;retention_value?:number;retention_unit?:string;version_count?:number;immutable:number|boolean;encrypted:number|boolean;owner:string;color:string};
export type AppData={locations:Entity[];sources:Entity[];targets:Entity[];datasets:Entity[];software:Entity[];plans:Plan[];settings:Record<string,string>;analysis:{score:number;tone:string;datasets:Array<{id:string;name:string;priority:string;score:number;checks:Array<{key:string;points:number;ok:boolean}>;planCount:number}>;counts:{plans:number;datasets:number;issues:number}};meta:{version:string;repo:string}};
