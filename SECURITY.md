# Security policy

Backup Planner is designed for a trusted private network and intentionally has no authentication. Do not expose it directly to the public internet. Report vulnerabilities privately through GitHub Security Advisories rather than a public issue. Supported security fixes target the latest released version.

The app does not read or execute backup data. Its GitHub release check is its only routine external request.
