export async function api<T=any>(url:string,options?:RequestInit):Promise<T>{const res=await fetch('/api'+url,{headers:{'content-type':'application/json',...(options?.headers||{})},...options});if(!res.ok){const body=await res.json().catch(()=>({}));throw new Error(body.error||`Request failed (${res.status})`)}const result=await res.json();if(options?.method&&options.method!=='GET'&&(url.startsWith('/backups')||url==='/import'))window.dispatchEvent(new CustomEvent('backup-planner:system-action'));return result}
