export async function api<T=any>(url:string,options?:RequestInit):Promise<T>{const headers:Record<string,string>={...(options?.headers as Record<string,string>||{})};if(options?.body&&!headers['content-type']&&!headers['Content-Type'])headers['content-type']='application/json';const res=await fetch('/api'+url,{...options,headers});if(!res.ok){const body=await res.json().catch(()=>({}));throw new Error(body.error||`Request failed (${res.status})`)}const result=await res.json();if(options?.method&&options.method!=='GET'&&(url.startsWith('/backups')||url==='/import'))window.dispatchEvent(new CustomEvent('backup-planner:system-action'));return result}
